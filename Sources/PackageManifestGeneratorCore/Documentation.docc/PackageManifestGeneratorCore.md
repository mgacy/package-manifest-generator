# ``PackageManifestGeneratorCore``

Invoke the core logic of the Package Manifest Generator tool.

@Metadata {
    @PageImage(
        purpose: icon,
        source: "logo",
        alt: "An icon representing Mobelux."
    )
    @PageColor(gray)
    @Available(macOS, introduced: "13.0")
}

## Overview

This library contains the core logic powering the Package Manifest Generator command-line tool.

## Topics

### Essentials
